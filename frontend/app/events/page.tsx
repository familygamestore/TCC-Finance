'use client';
import { useState } from 'react';
import { useEvents } from '@/hooks/useEvents';
import EventForm from '@/components/events/components/EventForm';
import EventList from '@/components/events/components/EventList';
import ErrorMessage from '@/components/events/components/ErrorMessage';
import { getAuthRole } from '@/lib/api';
export default function EventsPage(){const {events,brands,error,setError,form,setForm,submitting,loading,submitEvent,deleteEvent,refresh}=useEvents();const [showForm,setShowForm]=useState(false);const role=getAuthRole();return <div><div className="page-header"><div><div className="eyebrow">Operations · Universal Event Hub</div><h1>Event Hub</h1><p className="subtitle">Kelola event lintas game, kategori, jumlah tim, registrasi, budget dan prize pool dalam satu workspace.</p></div><div className="actions"><span className="live-pill"><span className="status-dot"/>{role==='SUPER_ADMIN'?'Super Admin · Full access':'Admin · Assigned access'}</span><button className="btn secondary" onClick={()=>void refresh()} disabled={loading}>↻ Refresh</button><button className="btn" onClick={()=>setShowForm(true)}>＋ Buat Event</button></div></div><ErrorMessage message={error} onClose={()=>setError('')}/><section className="event-toolbar"><div className="event-stats"><div><span>Total event</span><strong>{events.length}</strong></div><div><span>Upcoming</span><strong>{events.filter(x=>['upcoming','UPCOMING'].includes(x.status)).length}</strong></div><div><span>Budget</span><strong>{new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(events.reduce((s,x)=>s+Number(x.budget||0),0))}</strong></div></div><div className="helper">Super Admin dapat membuka dan mengelola seluruh event. Admin dibatasi berdasarkan akses brand.</div></section><EventList events={events} onDelete={deleteEvent} loading={loading}/>{showForm&&<><button className="drawer-backdrop" onClick={()=>setShowForm(false)} aria-label="Tutup"/><aside className="form-drawer event-drawer"><div className="form-drawer-head"><div><div className="eyebrow">Event Builder</div><h2>Buat Event Baru</h2><p className="subtitle">Isi hanya yang diperlukan. Field lainnya dapat dikembangkan kemudian.</p></div><button className="icon-button" onClick={()=>setShowForm(false)}>×</button></div><EventForm form={form} setForm={setForm} onSubmit={async f=>{await submitEvent(f);setShowForm(false)}} submitting={submitting} brands={brands}/></aside></>}</div>}
