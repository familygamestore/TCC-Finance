'use client';
import {useEffect,useState} from 'react';import{api,getAuthRole,Transaction}from '@/lib/api';import{formatRupiah}from '@/utils/formatters';
export default function TransactionsPage(){const[role,setRole]=useState('');const[rows,setRows]=useState<Transaction[]>([]);useEffect(()=>{const r=getAuthRole();setRole(r);if(r==='SUPER_ADMIN'||r==='ADMIN')api.getTransactions().then(setRows).catch(()=>setRows([]));},[]);if(!role)return <div className="loading">Memeriksa akses…</div>;if(!['SUPER_ADMIN','ADMIN'].includes(role))return <div className="auth-wrap"><div className="auth-card"><h1>Login diperlukan</h1><button className="btn" onClick={()=>location.href='/admin'}>Login</button></div></div>;return <div><div className="page-header"><div><div className="eyebrow">Read Only Ledger</div><h1>Transaksi</h1><p className="subtitle">{role==='SUPER_ADMIN'?'Seluruh ledger tersedia.':'Hanya transaksi dari brand yang diizinkan.'}</p></div></div><section className="section"><div className="table-wrap"><table><thead><tr><th>ID</th><th>Tanggal</th><th>Tipe</th><th>Nama</th><th>Kategori</th><th>Nominal</th></tr></thead><tbody>{rows.map(t=><tr key={t.transaction_id}><td>{t.transaction_id}</td><td>{t.tanggal} {t.jam}</td><td><span className={`tag ${t.type}`}>{t.type}</span></td><td>{String(t.nama_transaksi||t.nama_pengeluaran)}</td><td>{t.kategori}</td><td>{formatRupiah(t.nominal)}</td></tr>)}{!rows.length&&<tr><td colSpan={6} className="empty muted">Belum ada transaksi yang dapat dilihat.</td></tr>}</tbody></table></div></section></div>}
